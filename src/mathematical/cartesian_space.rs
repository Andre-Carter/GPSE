pub struct coordiantes {
    x: i32,
    y: i32,
    z: i32,
}

pub struct x_coordiante {
    x: i32,
    y: i32,
    z: i32,
}

pub struct y_coordiante {
    x: i32,
    y: i32,
    z: i32,
}

pub struct z_coordiante {
    x: i32,
    y: i32,
    z: i32,
}

let origin = coordinates = coordinates {
    x: 0.0,
    y: 0.0,
    z: 0.0,
}

//SIZE, or number of points in from orgin

//AXES

//SCALE